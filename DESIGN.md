# Simulator中必须的内容

## ROB
（重排序缓冲）
```
struct ROBEntry {
    bool occupied;          // 该条目是否有效
    uint8_t op_type;        // 0=ALU, 1=Branch, 2=Store, 3=Load, 4=NOP
    int dest_reg;           // 目的寄存器号，若无则为 -1
    uint32_t value;         // 计算结果（写回阶段填入）
    bool ready;             // 结果是否已写回（CDB广播到达）
    uint32_t store_addr;    // Store专用：目标内存地址
    uint32_t store_value;   // Store专用：要写的数据
    uint32_t pc;            // 该指令的PC（用于分支恢复）
    bool is_branch_taken;   // 预测方向（用于提交时确认是否正确）
};

// simulator
ROBEntry ROB[N];
```
每次从CDB接受更新消息，消耗一个周期
如果我们发现队首出现了ready，那么就进行寄存器的写入，或者去写入内存（LSQ）

如果issue了一条命令，那么通过判断RS ROB是否full来决定是否issue，从而判断PC是否变化
```
void cycle() {
    // 1. 先尝试提交（Commit）——释放 ROB 空间
    commit();

    // 2. 再尝试写回（Write Result）——填充 ROB 的 ready 位
    write_back();

    // 3. 最后尝试发射（Issue）——根据当前空位决定是否插入新元素
    if (!rob.is_full() && !rs.is_full() && fetch_buffer.has_inst()) {
        issue_instruction();
    } else {
        stall_pc = true; // PC 停住，不取指
    }
}
```

## ReservationStation
（保留站，内部可细分为 ALU_RS 和 MEM_RS）
```
struct RS_Entry {
    bool busy;         // 是否当前占用，只要里面还存有数据，就是busy状态
    int op;            // 操作类型
    int vj, vk;        // 操作数值
    int qj, qk;        // 依赖标签 (-1 表示就绪)
    int rob_tag;       // 对应的 ROB 条目编号（用于比较年龄）
    int cycles_left;   // 剩余执行周期数（0 表示空闲/刚就绪，>0 表示正在执行中）
    bool result_ready;   // 结果已算出，等待广播，广播冲突而需要仲裁的情景，而且只能放入下一时刻的广播队列中，仍然有可能广播不出来
    int pending_value;   // 等待广播的结果
};
```


## LSQ
（访存队列，必须单独拎出来）
也是在发射（Issue）阶段，当一条 lw（加载）或 sw（存储）指令被发射时，同时占用一个 ROB 条目和一个 LSQ 条目。

```
struct LSQEntry {
    bool occupied;          // 是否有效
    uint8_t op_type;        // LOAD 或 STORE
    uint32_t addr;          // 算出来的内存地址（执行阶段填入）
    bool addr_ready;        // 地址是否已算好（执行阶段置1）
    uint32_t value;         // LOAD：从内存读回的数据；STORE：要写的数据
    bool value_ready;       // 数据是否就绪（STORE依赖的寄存器值是否到了）
    uint32_t rob_index;     // 对应ROB里的条目索引（建立关联）
    bool is_committed;      // 是否已提交（仅用于状态机）
};
```


## RegisterFile
（含状态表 busy/tag，或者把状态表合并进这里）

- 每次issue发布的时候，需要更新写入寄存器的状态表对应值，记录来自哪条issue和哪个RS
- 每次CDB的时候，查找状态表中是否有对应的I，然后清空，并指向对应的RS，修改RS
- 分支预测错误，进行flush操作


## CDB
（公共数据总线，其实可以只是一个 broadcast_tag 和 broadcast_value 的临时变量，不需要类，但为了整洁可以封装）

每次找到每一个需要更新的值，然后写入ROB，通过ROB写入寄存器

其中可能来自ALU，可能来自内存读取Load，还有来自比较跳转，故需要仲裁机制。

目前考虑先发射Load出来的内容Load > ALU > 跳转

```
struct CDB_Packet {
    bool valid;
    int tag;    // 生产者标签（对应 ROB 条目索引）
    int value;  // 算出的结果
};
```

## Memory
（内存，模拟延迟）
约定读写内存均为3个cycle，使用store-RS的cycle-left进行判断当前操作是否做完

## Predictor
（分支预测器）
分支（Branch）和跳转（JAL/JALR）的投机执行与回退，是 Tomasulo 模拟器里**最容易出 bug 的地方**。 

讲义第 19 页专门强调：“嵌套投机路径是真正需要面对的状态管理问题”。为了让你一次性理清，我们把整个过程拆解为 **“预测 → 执行 → 解决 → 回退/提交”** 四个阶段，并特别指出**寄存器状态表（RAT）必须回滚**这个容易被忽视的致命细节。

---

### 1. 预测（取指阶段）
- **谁来做**：取指单元（IFU）中的分支预测器。
- **做什么**：遇到 `beq` / `bne` 时，根据预测方向（如“始终不跳转”或“BTFN”）决定下一个 PC。
- **关键动作**：预测结果（预测跳转/不跳转，以及预测目标地址）会随指令一起流水，最终存入 **ROB 分支条目**中。

---

### 2. 投机执行（发布 & 执行阶段）
- **原则（铁律）**：分支**后面的所有指令**，虽然正常进行“发射（Issue）”、“执行（Execute）”、“写回（Write Result）”，但**它们的 `ready` 状态只能用来唤醒依赖者，绝对不能把结果写入寄存器堆或内存**（PPT 第 16 页）。
- **保障机制**：这些投机指令的结果只填充进 **ROB 的 `value` 字段**，且 `ready=1`。它们排在分支后面，死死卡在 ROB 里，等待分支的“最终审判”。

---

### 3. 分支结果解决（写回阶段）
当分支指令进入 ALU 执行，计算出**实际跳转方向**和**实际目标地址**后：

- 分支将自己的结果（实际是否跳转）**通过类似CDB的一条专用通路广播**给对应的 ROB 条目。
- ROB 收到后，将分支条目的 `ready` 置为 1，并记录下 `actual_taken` 和 `actual_target`。
- **此时，分支依然不能提交**，必须等它排到 ROB 队首。

---

### 4. 审判时刻：提交（Commit）或 回退（Flush）
当分支成为 **ROB 队首** 时，执行以下判断（PPT 第 18 页）：

#### 情况 A：预测正确 ✅
- 分支正常提交（释放 ROB 队首）。
- **头指针（Head）后移**，分支后面那些投机执行的指令（如果它们已经 `ready=1`）会在接下来的周期里**依次正常提交**，合法地更新寄存器堆和内存。

#### 情况 B：预测错误 ❌（重点！必须按顺序做 4 件事）

**第 1 步：清空 ROB 中的错误路径**
- 将 **分支条目之后的所有 ROB 条目** 标记为无效（`occupied = false`）。
- **重要**：ROB 的 Tail 指针直接回退到 `分支条目索引 + 1` 的位置（相当于把错误路径从队列里物理切除）。

**第 2 步：清除保留站（RS）中的错误指令**
- 遍历所有 RS，清除那些 **“生产者标签”属于被清空 ROB 条目** 的等待项。
- 如果不清除，这些 RS 会永远等一个永远不会出现的标签，导致死锁。

**第 3 步：清除 LSQ 中的错误访存指令**
- 遍历 LSQ，清除所有比该分支年轻的 Load/Store 条目。
- 因为错误路径上的 Store **还没有提交（没写内存）**，所以清除它们对数据内存毫无影响（PPT 核心优势）。

**第 4 步：重置 PC 和 **【极易被忽视】恢复寄存器状态表（RAT）** 
- PC 重新指向 **正确的目标地址**（`actual_target`）。
- **⚠️ 致命细节**：分支预测错误后，**寄存器状态表（RAT）必须回滚（Restore）到分支之前的状态！**
  - 因为错误路径上的指令（比如 `add x5...`）在发布时，把 RAT 里 `x5` 的映射改成了自己的标签（覆盖了分支前的正确映射）。
  - 如果你只清空 ROB 而不恢复 RAT，后续正确路径的指令读取 `x5` 时，会继续等待那个已经被清空的错误标签，导致**永远卡死**。
  - 实现方法：维护一个 **RAT 检查点（Checkpoint）**，在分支发射时备份 RAT；回退时直接拷贝备份覆盖当前 RAT。

这里的实现应该是：所有的分支预测仅在ROB队头位置进行判断，故而一旦出现预测错误，可以直接进行RAT清空，因为生产者此时是空的

---

### 5. 特别关注：JAL / JALR（无条件跳转）
- **JAL**：本质是“必定跳转的分支”。预测器直接预测“跳转”，通常不会预测错。
- **JALR**（讲义第 10 页特别提及）：因为目标地址依赖寄存器（如 `jalr x1, x2, 0`），如果 `x2` 还没算出来，地址未知。
  - **处理方法**：在 `x2` 就绪前，**JALR 指令会卡在 RS 里不执行**（和普通 ALU 指令一样等数据）。地址算出来之前，**取指阶段必须停顿（Stall）**，不能冒险去取指，因为根本不知道下一条指令在哪。
  - 地址算出来后，执行阶段算出目标 PC，后续流程与普通分支完全一致。

---

### 6. 嵌套分支的“清空范围”（讲义第 19 页）
如果预测路径上又遇到第二个分支，ROB 里会有两个分支条目。

- 如果**第二个分支**预测错了，只清空 **ROB 中第二个分支之后** 的指令，**保留**第一个分支之后的指令（因为它们依然在正确路径上）。
- 如果**第一个分支**预测错了，**必须清空包括第二个分支在内的所有后续指令**。
- **模拟器实现技巧**：清空时，只要判断 `ROB 条目索引 > 错误分支的索引`，就把这些条目全部干掉。因为 ROB 是按顺序排列的，这个简单的下标比较就能完美区分嵌套层级。

---

### 给你的 C++ 模拟器“状态机”建议

在分支写回（Write Result）阶段，**不要立刻清空**！因为写回时分支可能还在 ROB 中间位置。正确的做法是：

1. 分支写回时，只在 ROB 里标记 `actual_taken` 和 `actual_target`。
2. 等到 **Commit 阶段**，当分支成为队首时，才执行“正确提交”或“触发 Flush”。
3. 执行 Flush 时，**务必记得把 PC 修改为 `actual_target`**，同时恢复 RAT 备份。

只要把 **RAT 回滚** 和 **PC 重定向** 这两个动作做对，投机执行就能完美运行，所有测试点都能顺利通过。如果对 RAT 检查点的数据结构（用栈还是数组）有疑问，随时可以继续问我。😊